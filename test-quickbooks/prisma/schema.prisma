model UserQuickBooksToken {
  id           String   @id @default(cuid())
  clerkUserId  String   @unique   // link to Clerk user
  realmId      String   // QuickBooks company ID
  accessToken  String
  refreshToken String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}